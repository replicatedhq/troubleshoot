apiVersion: troubleshoot.sh/v1beta2
kind: SupportBundle
metadata:
  name: skipped-collectors-test
spec:
  collectors:
    - clusterResources:
        exclude: true
    - clusterInfo:
        exclude: true
