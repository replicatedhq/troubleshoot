apiVersion: troubleshoot.sh/v1beta2
kind: SupportBundle
metadata:
  name: replicated-app
spec:
  collectors:
    - http:
        collectorName: replicated-app
        get:
          url: https://replicated.app
        tls:
          cacert: |-
